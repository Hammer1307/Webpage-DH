---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { SITE, BOOKING_OPTIONS, CONTACT, VENUE, IMAGES } from '@/data/site';

const description =
  'Kontakt und Buchung — kostenfreies Orientierungsgespräch, 1:1 Sitzung, KlarheitsReset oder Team-Vorgespräch mit Daniela Britta Hamberger.';

const breadcrumb = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Start', item: `${SITE.url}/` },
    { '@type': 'ListItem', position: 2, name: 'Kontakt', item: `${SITE.url}/kontakt` },
  ],
};
---
<BaseLayout
  title="Kontakt"
  description={description}
  schema={breadcrumb}
>
  <section class="page-hero">
    <div class="container">
      <nav class="breadcrumb" aria-label="Breadcrumb" style="text-align: left;">
        <a href="/">Start</a><span>/</span><span>Kontakt</span>
      </nav>
      <span class="eyebrow">Kontakt &amp; Buchung</span>
      <h1>Lass uns schauen, <em>was für dich passt</em></h1>
      <p class="lead">
        Du möchtest herausfinden, ob eine 1:1 Begleitung, ein KlarheitsReset oder ein
        Teamformat passend ist?
      </p>
    </div>
  </section>

  <section class="section section-cream">
    <div class="container-narrow">
      <p>
        Dann buche gerne ein kostenfreies Orientierungsgespräch. In 15–30 Minuten klären wir
        dein Anliegen, mögliche nächste Schritte und die passende Form der Zusammenarbeit.
      </p>
      <div class="btn-group">
        <a href="#kontaktformular" class="btn btn-primary">Kostenfreies Orientierungsgespräch buchen</a>
        <a href="#kontaktformular" class="btn btn-outline">Teamformat anfragen</a>
        <a href={`mailto:${CONTACT.email}`} class="btn btn-outline">Nachricht schreiben</a>
      </div>
    </div>
  </section>

  <section class="section section-cream-deep">
    <div class="container">
      <header class="section-head">
        <span class="eyebrow">Buchungsoptionen im Überblick</span>
        <h2>Vier Wege, <em>uns auszutauschen</em></h2>
      </header>
      <div class="booking-grid">
        {BOOKING_OPTIONS.map((b) => (
          <article class="booking-card">
            <h3>{b.title}</h3>
            <p class="duration">{b.duration}</p>
            <p>{b.text}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  {/* ============== RÄUMLICHKEITEN WIESBADEN ============== */}
  <section class="section bg-cream-soft venue-section">
    <div class="container venue-grid">
      <div class="venue-text">
        <span class="eyebrow">Kontakt &amp; Räumlichkeiten</span>
        <h2>Vor Ort in <em>Wiesbaden</em></h2>
        <p class="lead">
          Termine finden online, telefonisch oder nach Absprache vor Ort statt.
        </p>
        <p>
          Für ausgewählte Begleitungen, Workshops und Teamformate nutze ich stimmige
          Räumlichkeiten in Wiesbaden, unter anderem den <strong>{VENUE.name}</strong>.
        </p>
        <address class="venue-address">
          <strong>{VENUE.name}</strong><br />
          {VENUE.street}<br />
          {VENUE.postalCode} {VENUE.city}
        </address>
        <aside class="venue-note">
          <strong>Hinweis:</strong> Vor-Ort-Termine und Workshops werden ausschließlich
          über Daniela Britta Hamberger abgestimmt und gebucht — nicht extern über
          den Seminarraum.
        </aside>
        <div class="btn-group">
          <a href="#kontaktformular" class="btn btn-primary">Gespräch buchen</a>
          <a href={`mailto:${CONTACT.email}`} class="btn btn-secondary">E-Mail schreiben</a>
        </div>
      </div>
      <div class="venue-visual">
        <img
          src={IMAGES.raumMeditation}
          alt={`Meditationskreis im ${VENUE.name}, Wiesbaden`}
          loading="lazy"
          class="venue-img"
        />
      </div>
    </div>
  </section>

  <section class="section section-cream" id="kontaktformular">
    <div class="container-narrow">
      <h2>Schreib mir eine Nachricht</h2>
      <p>
        Du kannst mir auch direkt schreiben. Ich melde mich in der Regel innerhalb von
        1–2 Werktagen.
      </p>

      <form class="contact-form" action={`mailto:${CONTACT.email}`} method="post" enctype="text/plain">
        <div class="field">
          <label for="name">Name</label>
          <input type="text" id="name" name="name" required />
        </div>
        <div class="field">
          <label for="email">E-Mail</label>
          <input type="email" id="email" name="email" required />
        </div>
        <div class="field">
          <label for="topic">Anliegen</label>
          <select id="topic" name="topic">
            <option>1:1 Begleitung</option>
            <option>Teamformat</option>
            <option>Publikationen / Interessentenliste</option>
            <option>Sonstiges</option>
          </select>
        </div>
        <div class="field">
          <label for="message">Nachricht</label>
          <textarea id="message" name="message" rows="6" required></textarea>
        </div>
        <div class="field-check">
          <input type="checkbox" id="impulsbrief" name="impulsbrief" />
          <label for="impulsbrief">
            Ich möchte den Impulsbrief von Daniela Britta Hamberger erhalten und über
            neue Publikationen, Materialien und Angebote informiert werden.
          </label>
        </div>
        <button type="submit" class="btn btn-primary">Nachricht senden</button>
      </form>

      <p class="form-note">
        Alle Angaben werden vertraulich behandelt. Du kannst dich jederzeit vom Impulsbrief
        abmelden.
      </p>

      <blockquote class="disclaimer">
        <strong>Wichtiger Hinweis:</strong> Die Angebote auf dieser Website ersetzen keine
        medizinische, psychotherapeutische oder psychiatrische Behandlung. Mentoring und
        Coaching sind keine Therapie. Bei psychischen Erkrankungen oder Krisen wende dich
        bitte an eine Ärztin, einen Arzt oder eine zugelassene Psychotherapeutin bzw. einen
        zugelassenen Psychotherapeuten.
      </blockquote>
    </div>
  </section>
</BaseLayout>

<style>
  .booking-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
    gap: 1.25rem;
  }
  .booking-card {
    background: var(--color-paper);
    border: 1px solid var(--color-line);
    border-top: 3px solid var(--color-gold);
    padding: 1.5rem;
    border-radius: var(--radius-m);
  }
  .booking-card h3 {
    font-size: 1.1rem;
    margin-bottom: 0.4rem;
  }
  .booking-card .duration {
    color: var(--color-gold-deep);
    font-family: var(--font-display);
    font-style: italic;
    margin-bottom: 0.85rem;
  }
  .booking-card p { font-size: 0.92rem; margin: 0; }

  .contact-form {
    margin-top: 1.5rem;
    background: var(--color-paper);
    padding: clamp(1.5rem, 4vw, 2.5rem);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-m);
  }
  .field { margin-bottom: 1.25rem; }
  .field label,
  .field-check label {
    display: block;
    font-size: 0.88rem;
    font-weight: 500;
    color: var(--color-ink);
    margin-bottom: 0.4rem;
  }
  .field input,
  .field select,
  .field textarea {
    width: 100%;
    padding: 0.75rem 1rem;
    font: inherit;
    font-size: 0.95rem;
    background: var(--color-cream);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-s);
    color: var(--color-ink);
  }
  .field input:focus,
  .field select:focus,
  .field textarea:focus {
    outline: 2px solid var(--color-gold);
    outline-offset: 1px;
    border-color: var(--color-gold);
  }
  .field-check {
    display: flex;
    gap: 0.65rem;
    margin: 1.25rem 0 1.5rem;
    align-items: flex-start;
  }
  .field-check input {
    margin-top: 0.25rem;
    accent-color: var(--color-bordeaux);
  }
  .field-check label {
    margin: 0;
    font-weight: 400;
    color: var(--color-ink-soft);
    font-size: 0.88rem;
    line-height: 1.5;
  }
  .form-note {
    font-size: 0.83rem;
    color: var(--color-ink-muted);
    font-style: italic;
    margin-top: 1rem;
  }

  /* ============== VENUE WIESBADEN ============== */
  .venue-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: center;
  }
  .venue-text h2 { margin: 0.5rem 0 1rem; }
  .venue-text .lead { margin-bottom: 1.25rem; max-width: 38ch; }
  .venue-text p { margin-bottom: 1.25rem; max-width: 46ch; }
  .venue-address {
    background: var(--color-paper);
    border-left: 3px solid var(--color-clay);
    padding: 1.25rem 1.5rem;
    font-style: normal;
    line-height: 1.7;
    border-radius: 0 var(--radius-s) var(--radius-s) 0;
    margin-bottom: 1.25rem;
    color: var(--color-ink);
  }
  .venue-note {
    background: var(--color-cream-deep);
    padding: 1rem 1.25rem;
    font-size: 0.9rem;
    color: var(--color-ink-soft);
    border-radius: var(--radius-s);
    margin-bottom: 0.5rem;
  }
  .venue-note strong { color: var(--color-sage-deep); }
  .venue-img {
    aspect-ratio: 4 / 5;
    max-width: 28rem;
    width: 100%;
    object-fit: cover;
    border-radius: var(--radius-l);
    box-shadow: var(--shadow-lift);
    margin-inline: auto;
  }
  @media (max-width: 860px) {
    .venue-grid { grid-template-columns: 1fr; }
    .venue-img { max-width: 22rem; aspect-ratio: 4 / 3; }
  }
</style>
